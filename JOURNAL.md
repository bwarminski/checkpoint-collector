# Journal

- Task 1 foundation files under `load/lib/load/` now load the namespace and the base classes/value objects needed by later runner slices.
- `Load::RateLimiter` in the new runner uses `rate_limit: :unlimited` and keeps the shared mutex plus injected `clock`/`sleeper` contract from the fixture limiter.
- `Load::Selector` is a cumulative-weight picker over the injected seeded `Random`; the repeatability test only needs the selector to return `ActionEntry` objects.
- `Load::Scale` now defaults `open_fraction` to `nil` so workloads can omit it and still match the documented contract.
- Task 2 uses a root override in `Fixtures::Manifest.load` so tests can write temp fixture trees without touching repo fixtures.
- `Fixtures::Command` accepts `manifest_loader:` so command parsing can be tested without requiring real fixture implementations.
- `bin/fixture` is the top-level entrypoint and should stay thin; fixture behavior belongs under `collector/lib/fixtures/` and `fixtures/`.
- Task 2 command dispatch should treat handler failures as runtime errors, not usage errors.
- Invalid `--rate` input should be converted into an `OptionParser` parse error so the CLI prints usage instead of crashing.
- `bin/fixture missing-index reset` currently loads the full registry and therefore requires Task 4/5 files to exist before the CLI can run successfully.
- The fixture command registry now defers each fixture file `require` until that verb runs, so `reset` can work before `drive` and `assert` exist.
- The missing-index template must create `pg_stat_statements` itself; otherwise `fixture_01` clones successfully but `pg_stat_statements_reset()` fails at the end of reset.
- If template bootstrap fails after creating `fixture_01_tmpl`, reset must drop the template before re-raising so later runs do not reuse a poisoned database.
- Task 4 drive code uses a per-thread `RateLimiter` instance instead of sharing one limiter across worker threads.
- The live `bin/fixture missing-index drive --seconds 1 --concurrency 2 --rate unlimited` smoke run against a tiny local HTTP server wrote `tmp/fixture-last-run.json` with `request_count: 1194` and no stdout/stderr output.
- Task 4 fix: the drive path now shares one synchronized limiter across workers and rescues worker exceptions inside the run loop before re-raising in the main thread.
- Task 5 live `EXPLAIN` output on `fixture_01` has a `Gather` root with the `Seq Scan` on `todos` beneath it, so the assertion needs to walk the plan tree instead of assuming the scan is the top node.
- The real `bin/fixture missing-index assert --timeout-seconds 180` run in this environment completed the explain check and then timed out with `ClickHouse saw only 0 calls before timeout`.
- Task 5 output should stay structured: `FIXTURE: missing-index`, then detailed `PASS: explain (...)` and `PASS: clickhouse (...)` lines.
- Task 6 adds a manual `make fixture-smoke` target that runs `bin/fixture missing-index all`; without the external demo app running it prints the underlying command, shows the Postgres `fixture_01` notice if needed, and then waits until interrupted or timed out.
- 2026-04-19 verification: `BUNDLE_GEMFILE=collector/Gemfile bundle exec ruby collector/test/fixtures/fixture_manifest_test.rb`, `fixture_command_test.rb`, `missing_index_reset_test.rb`, `missing_index_drive_test.rb`, `missing_index_assert_test.rb`, and `fixture_smoke_target_test.rb` all passed; `ruby load/test/harness_test.rb` passed; `python3 -m pytest tests/smoke/test_collector_pipeline.py -v` failed in setup because Docker could not bind ClickHouse on `0.0.0.0:8123`; `bin/fixture missing-index all --rebuild-template --timeout-seconds 180` timed out waiting for `http://localhost:3000/up`.
- 2026-04-19 verification follow-up: forcing `COMPOSE_PROJECT_NAME=checkpoint-collector` in the smoke helpers makes pytest reuse the intended stack from a worktree, and the first smoke test needed to poll for its specific `pg_stat_statements` row instead of any `postgres_logs` row to avoid a clean-stack ingestion race.
- `bin/fixture missing-index reset --rebuild-template` is not deadlocked when quiet; on a clean volume it spends a few minutes actively building `fixture_01_tmpl`, and `todos` reaches roughly 1 GB before the command returns.
- The current `ab679e7` baseline in `~/db-specialist-demo` has no `/up` route; `curl http://127.0.0.1:3000/up` returns `404`, so `bin/fixture missing-index drive` and `all` will time out in readiness unless the demo app contract changes.
- Readiness timeout errors now include the last observed health result, so this failure surfaces as `last status: 404` instead of looking like an unexplained hang.
- 2026-04-20 planning: the Load Runner MVP replaces the fixture harness on `wip/fixture-harness-plan`; the implementation plan needs to keep the tree green by building `/load` core first, then the Rails adapter, then the workload/oracle, and only then deleting `bin/fixture`, `fixtures/missing-index/`, `collector/lib/fixtures/`, `collector/test/fixtures/`, `load/harness.rb`, `load/README.md`, and `fixture-harness-walkthrough.md`.
- Task 2 implementation uses a minimal file-backed `Load::RunRecord` with `run.json`, `metrics.jsonl`, and `adapter-commands.jsonl`; the load tests currently exercise only the write/append contract, not the later runner orchestration.
- Task 2 reporter fix: `Load::Reporter` now starts a background interval loop, sleeps through the injected interval contract, and still flushes tail data on `stop` even if `start` was never called.
- Task 2 metrics shape now includes `p50_ms`, `p95_ms`, `p99_ms`, and `max_ms`, and reporter snapshot lines use `ts` plus `interval_ms` to match the later JSONL contract.
- Task 2 reporter loop now sleeps before the first periodic snapshot; the only early flush remains `stop`, and `interval_ms` is emitted as an integer.
- Task 2 reporter shutdown now kills the waiting thread before join, so `stop` returns promptly instead of waiting for the interval sleeper to finish.
- Task 2 reporter hardening replaced the hard kill with cooperative `Shutdown` handling that only interrupts the sleeper phase; in-flight snapshots finish before exit, and worker error labeling now falls back to `:unknown` if `action.name` itself raises.
- Task 3 uses a thin `Load::CLI` seam with injected runner callables, a JSON-speaking `Load::AdapterClient` wrapper, and a runner that pins `window.start_ts` from the first successful worker response while still always stopping the adapter.
- Task 3 readiness state now lives under `window.readiness`, and `window.end_ts` is written before the final outcome so the run record carries the full window envelope.
- The zero-seed runner test is stable when the seeded action triggers the shared stop flag after recording the first RNG value.
- Task 3 adapter invocations now force `--json` so the wrapper is not relying on adapter default output mode, and the zero-seed test yields before advancing the fake clock so the worker thread can record deterministically.
- Task 4 now has a working `adapters/rails/` scaffold with green command-unit tests and a `bench-adapter` binary that preserves the JSON error contract even for unknown commands and parse failures.
- Task 5 integration exposed three real adapter boundary bugs: `prepare` must not inherit the outer `bundle exec` Gemfile, long-lived `start` processes need their own process group, and `TemplateCache` must lazy-load `pg` so the SQLite fixture app can still run.
- The Rails adapter's `migrate` contract is schema canonicalization, not literal migration replay; `db-specialist-demo` has no migration files, so the working implementation is `db:create db:schema:load`, and `reset-state` mirrors that before explicit seeding.
- `db-specialist-demo` needed a benchmark-facing `db/seeds.rb`, a `benchmark` database config, and a repaired `bin/rails` entrypoint shebang; the checked-in `db/schema.rb` remained the canonical schema and had to be restored after local failed setup attempts clobbered it.
- The Postgres compose service initially came up healthy without publishing `5432`; forcing a recreate restored the host port binding, and `reset-state` also has to `CREATE EXTENSION IF NOT EXISTS pg_stat_statements` before calling `pg_stat_statements_reset()`.
- Task 6 workload parity points carried over from the fixture harness are `GET /todos/status?status=open`, `16` workers, `60` seconds, and unlimited rate; the old fixture manifest is the source of truth for those numbers.
- The tactical oracle should tree-walk EXPLAIN JSON for a `Seq Scan` on `todos`, but ClickHouse polling must read from `query_intervals`, not raw `query_events`, or cumulative counters will overstate call counts across multiple scrapes.
- Task 8 verification exposed a real `bin/load` entrypoint bug: the script shifted `run` before handing `ARGV` to `Load::CLI`, so the documented command always exited with usage; a script-level CLI test now covers the real `bin/load run ...` path.
- Task 8 verification also exposed a repo contract gap: `make load-smoke` and the top-level README need `DATABASE_URL=postgres://postgres:postgres@localhost:5432/checkpoint_demo` and `BENCH_ADAPTER_PG_ADMIN_URL=postgres://postgres:postgres@localhost:5432/postgres` in front of `bin/load run`, because the Rails adapter template cache depends on those env vars.
- The Rails adapter template cache must key template databases by schema digest, not just database name; otherwise switching the benchmark app to a different branch silently reuses a stale template and parity checks lie.
- The raw `oracle/add-index` tag is not sufficient under the `db:schema:load` adapter contract: it adds a migration file but leaves `db/schema.rb` without the `todos.status` index, so the negative control needs a temporary branch that also materializes the indexed schema.
- 2026-04-21/22 parity SHAs: `~/db-specialist-demo` baseline `wip/load-benchmark-seeds` at `1a49917`; negative control branch `parity/oracle-add-index-benchmark-seeds` at `d209413` (oracle tag + benchmark seeds + `/up` + indexed `db/schema.rb`).
- 2026-04-22 parity baseline run #1: `runs/20260422T020136Z-missing-index-todos` completed and `ruby workloads/missing_index_todos/oracle.rb ...` reported `PASS: explain (Seq Scan on todos, plan node confirmed)` and `PASS: clickhouse (5730 calls; mean 0.1ms)`.
- 2026-04-22 parity baseline run #2: `runs/20260422T020249Z-missing-index-todos` completed and the oracle again reported `PASS: explain (Seq Scan on todos, plan node confirmed)` and `PASS: clickhouse (5377 calls; mean 0.1ms)`.
- 2026-04-22 negative control debugging: the first indexed-branch attempt timed out on readiness because `oracle/add-index` predates `/up`; after cherry-picking the liveness endpoint it still falsely passed until the schema-digest cache fix landed and the temporary branch materialized the indexed schema in `db/schema.rb`.
- 2026-04-22 negative control final run: `runs/20260422T020902Z-missing-index-todos` completed against `parity/oracle-add-index-benchmark-seeds`, and the oracle correctly exited `1` with `FAIL: explain (expected Seq Scan, got Index Scan)`.
- Task 9 removed the old fixture harness (`bin/fixture`, `collector/lib/fixtures/`, `collector/test/fixtures/`, `fixtures/missing-index/`) and the toy harness files (`load/harness.rb`, `load/test/harness_test.rb`, `load/README.md`, `fixture-harness-walkthrough.md`) after parity was already locked.
- Post-deletion verification stayed green: `load/test/rate_limiter_test.rb`, `load/test/metrics_test.rb`, `load/test/runner_test.rb`, `adapters/rails/test/integration_test.rb`, and `workloads/missing_index_todos/test/oracle_test.rb` all passed or skipped as expected, and `rg` found no live runtime references to `bin/fixture`, `Fixtures::`, or `load/harness.rb`.
- 2026-04-22 post-deletion parity rerun: `runs/20260422T021532Z-missing-index-todos` completed against `wip/load-benchmark-seeds`, and the oracle still reported `PASS: explain (Seq Scan on todos, plan node confirmed)` and `PASS: clickhouse (5629 calls; mean 0.1ms)`.
- Final review fix: `RailsAdapter::Commands::ResetState` now derives the template-cache database name from `DATABASE_URL` when present, so non-default benchmark database URLs do not accidentally keep cloning `checkpoint_demo`.
- Final review fix: `Load::Runner` now passes the injected `http:` client through to worker-side `Load::Client` instances, so the actual load path is testable without real sockets and matches the readiness-probe injection contract.
- 2026-04-22 CLI cleanup decision: workload selection is now `--workload <name>` plus explicit `Load::WorkloadRegistry` registration from `workloads/<name.tr("-", "_")>/workload.rb`; we are intentionally dropping the old `--workload PATH` plus `ObjectSpace` class discovery because it was global-state magic and made the CLI harder to reason about.
- 2026-04-23 runner cleanup: `Load::Runner` now uses one readiness concept (`startup_grace_seconds`), normalizes disabled readiness to `nil` at the CLI boundary, delegates probe timing/payload assembly to `Load::ReadinessGate`, uses named `TrackingBuffer`/`MetricsSink` helpers instead of dynamic singleton/class generation, and handles adapter stop failures through an explicit local result instead of returning from `ensure`.
- 2026-04-23 runner follow-up: runner config/dependencies are grouped into `Settings` and `Runtime`, process exit values live in `Load::ExitCodes`, and worker drain no longer raises `DrainTimeout` into worker threads because `Worker#run` catches `StandardError` and could turn forced shutdown into synthetic request errors.
- 2026-04-23 ship-review round: `Load::RateLimiter` must reserve the next slot under the mutex and sleep after releasing it; the earlier implementation held the lock across `sleep`, which serialized workers and hid behind the fake clock test until a wall-clock overlap test exposed it.
- 2026-04-23 ship-review round: workers now own a started `Load::Client` for the full run loop and always `finish` it in `ensure`; readiness still uses the short-lived request path because it never enters the worker lifecycle and only probes once.
- 2026-04-23 ship-review round: `RailsAdapter::TemplateCache` must key template databases by the full seed-time environment, including `SEED`, not just the schema digest; otherwise clone hits silently restore the wrong dataset for later benchmark runs on the same schema.
- 2026-04-24 ship-review round: request totals now live in each worker-owned `TrackingBuffer` and are aggregated when the runner writes outcome state; only the first-success window pin touches the runner's shared state mutex on the request path.
- 2026-04-24 ship-review round: the remaining cleanup aligned the contract and artifacts with the ship review — reporter tail intervals now use elapsed time, template-cache database names are validated before SQL interpolation, adapter command logs redact secrets, `run.json` carries `schema_version: 1`, and the spec now matches the implemented `migrate`/`reset-state`/`run.json` contract without reviving the dropped `--debug-log` path.
- 2026-04-24 plan-eng-review round 4: `Load::Runner` must build one `Load::Client` per worker, not one shared client per run; otherwise workers share a single `Net::HTTP` session and the first worker shutdown can close the socket out from under the rest of the run.
- 2026-04-24 manual smoke debugging: when `make load-smoke` times out, the useful operator workflow is adapter-driven reset/start plus direct `curl` and `xargs -P` probes; the top-level README now documents how to vary seed size and concurrency outside `bin/load run`, which still uses workload-declared scale.
- 2026-04-24 run-artifact docs: the README now explains the intent of `run.json`, `metrics.jsonl`, and `adapter-commands.jsonl`, plus the signals that distinguish setup delay, graceful degradation, and full saturation during smoke runs.
- 2026-04-24 local verification ergonomics: `Makefile` now exposes `make test` plus `test-load`, `test-adapters`, and `test-workloads`, and the README distinguishes that code-level verification path from the environment-dependent `make load-smoke` path.
- 2026-04-24 adapter integration ergonomics: the opt-in adapter integration coverage needs separate Make targets for the fixture Rails app and the real `db-specialist-demo` app because the fixture path is SQLite-backed while the demo path requires `DATABASE_URL` and `BENCH_ADAPTER_PG_ADMIN_URL` for Postgres.
- 2026-04-24 richer fixture design: the next diagnosis scenario keeps the `missing-index-todos` name and narrow oracle, but replaces the single-action fixture with a TodoMVC-shaped JSON API plus mixed traffic in `db-specialist-demo`; the dominant issue remains the missing `todos.status` index, while count-side N+1 and the old search/query-shape issue stay present as background pathologies.
- 2026-04-24 Task 1 CLI contract: `Load::CLI` now recognizes `run`, `soak`, and `verify-fixture`; `run` maps to `mode: :finite`, `soak` maps to `mode: :continuous`, and `verify-fixture` forwards `workload_name`, `adapter_bin`, and `app_root` into the injected verifier factory.
- 2026-04-24 Task 1 note: `Load::ExitCodes` still exposes `SUCCESS`, so the soak test asserts that constant rather than introducing an `OK` alias in an unrelated file.
- 2026-04-24 Task 1 follow-up: `verify-fixture` now rejects runner-only flags through the shared option parser and defaults to a rescueable `ArgumentError` path instead of `NotImplementedError`.
- 2026-04-24 mixed missing-index workload: the workload stays named `missing-index-todos`, uses `rows_per_table: 100_000` and `open_fraction: 0.6`, and the default action mix is 68/12/7/7/3/2/3 across list-open, list-recent, create, close, delete-completed, fetch-counts, and search.
- 2026-04-24 mixed missing-index workload follow-up: `CreateTodo` needed `Load::Client#request` body support so the new action tests can assert a minimal JSON payload without adding separate verb helpers.
- 2026-04-24 Task 4 demo-app TDD: `db-specialist-demo` controller tests only reach the intended red/green cycle under `SECRET_KEY_BASE=test RAILS_ENV=benchmark`; the default `test` env still boots SQLite and dies in `db/seeds.rb` on Postgres-only `TRUNCATE`.
- 2026-04-24 Task 4 follow-up: the `/api/todos/counts` test now proves N+1 by asserting the per-user count-query family scales with the number of users under test, and the `/api/todos/search` matcher now checks for `title` + `LIKE` + `%term%` across SQL/bind variations instead of relying on one rendered literal string.
- 2026-04-25 Task 6 verifier: `Load::FixtureVerifier` now takes injected explain/stat readers plus a client factory, verifies `/api/todos?status=open`, `/api/todos/counts`, and `/api/todos/search?q=foo` against the mixed fixture contracts, and its default PG-backed counts check uses the updated conservative floor of at least 2 distinct queryids after one isolated counts request.
- 2026-04-25 Task 7 runner gate: `Load::Runner` now accepts an optional verifier plus `mode`, runs the verifier only for finite runs after readiness and before workers, and maps verifier drift to `outcome.error_code = "fixture_verification_failed"` while still stopping the adapter and leaving `metrics.jsonl` absent because workers never started.
- 2026-04-25 Task 7 follow-up: CLI now injects `Load::FixtureVerifier` only for `missing-index-todos` runs, including `soak`, `Load::Runner` runs any injected verifier after readiness regardless of mode, and top-level `verify-fixture` drift exits as `ADAPTER_ERROR` without printing usage.
- 2026-04-25 Task 7 narrow follow-up: `verify-fixture` now runs the real adapter lifecycle (`describe`/`prepare`/`reset-state`/`start`/readiness/verify/`stop`), verifier setup/runtime failures are classified as `ADAPTER_ERROR` instead of usage errors, and `cli_test` now has subprocess coverage proving `soak --workload missing-index-todos` still wires the real verifier path.
- 2026-04-25 Task 8 soak semantics: `Load::Runner` now samples invariants only in `mode: :continuous`, aborts on 3 consecutive breach samples, resets the breach counter on any healthy sample, and persists breach warnings inside `run.json` so later state writes do not drop them.
- 2026-04-25 Task 8 race fix: sampler warnings and first-success window pin both rewrite `run.json`, so every runner-side `write_run` call must stay under the same mutex; otherwise concurrent writes fight over the shared `run.json.tmp` path and lose warnings or raise `ENOENT`.
- 2026-04-25 Task 8 follow-up: the continuous-run sampler now sleeps before its first invariant sample, interrupts only the sleep phase on shutdown so `join` does not sit on the full interval, converts sampler thread crashes into `outcome.error_code = "invariant_sampler_failed"`, and lets `finish_run` prefer `invariant_breach` over `no_successful_requests` even when no worker request completed.
- 2026-04-25 narrow verifier bugfix: `Load::FixtureVerifier` must treat `fixtures/mixed-todo-app/search-explain.json` as a rewrite-shape reference, not an exact planner-estimate snapshot; the search check now compares only stable structural fields (`Node Type`, `Plans`, `Relation Name`, `Sort Key`, `Filter`) so live cost and row drift does not fail `/api/todos/search`.
- 2026-04-25 Task 11 operator surface: `Makefile` now exposes `verify-fixture` and `load-soak`, and the README documents the direct `bin/load verify-fixture` / `bin/load soak` commands alongside the existing smoke flow.
- 2026-04-25 real mixed-workload verification: `reset-state` query-id capture must fingerprint the `/api/todos?status=open` query shape with `ORDER BY created_at DESC, id DESC LIMIT/OFFSET`; the old plain `WHERE status = ...` fingerprint caused the oracle to wait on a statement family that never appeared in ClickHouse.
- 2026-04-25 real mixed-workload tuning: with `rows_per_table: 100_000`, the counts N+1 path was too expensive at weight `6` and collapsed the dominance margin; lowering `FetchCounts` to weight `2` restored a real finite run to `744` ClickHouse calls on the primary query and a `4.32x` dominance margin over the next queryid.
- 2026-04-25 round-2 review fix: mixed write actions must sample `user_id` and `todo_id` from the worker RNG via `ctx[:scale]`; otherwise create/close/delete traffic degenerates to user/todo `1` and the soak stationarity math is invalid even if short runs look fine.
- 2026-04-25 soak verification fix: `bin/load soak` was accidentally constructing `Load::Runner` without `mode: :continuous`, so it silently behaved like a 60-second finite run. The real degraded soak evidence now comes from `runs/20260425T154248Z-missing-index-todos`, which persisted three invariant samples plus three warnings before exiting with `error_code: "invariant_breach"` after `TRUNCATE todos`.
- 2026-04-25 operator docs: the top-level README now has one run-modes section covering `bin/load run`, `bin/load soak`, and `bin/load verify-fixture`, including intent, artifact expectations, and sample output drawn from the real finite and degraded-soak runs on this branch.
- 2026-04-25 refactor design: push `open_fraction` and invariant SQL/thresholds out of `Load::Scale` and `Load::Runner`; make `InvariantSample` generic as a collection of named checks, add `Load::Workload#invariant_sampler(database_url:, pg:)`, and let `missing-index-todos` own its `extra` env pairs plus sampler thresholds and SQL.
- 2026-04-25 runner decomposition design: keep `Load::Runner` as the top-level use-case shell, but extract `RunState`, `LoadExecution`, and `InvariantMonitor` as the first real boundary cuts; do not widen scope into `AdapterSession` yet.
- 2026-04-25 Task 1 extraction start: `RunState` is being extracted next; the focused spec needs to cover isolated snapshot copies because the runner currently mutates shared state under a mutex and the new boundary must keep run.json writes from aliasing caller-owned data.
- 2026-04-25 Task 1 extraction result: `TrackingBuffer` now pins window start through `runner.run_state` and must call `current_time` through `send`; otherwise the private-method boundary breaks worker requests and the run falls back to `no_successful_requests`.
- 2026-04-25 Task 1 review fix: `RunState` now has regression coverage for nested caller-owned payload isolation on `merge`, `append_warning`, and `append_invariant_sample`, and the runner no-success path is anchored to keep writing `window.end_ts`.

- 2026-04-25 Task 1: moved workload knobs into `Load::Scale#extra`; `Scale` now validates reserved keys and emits only `ROWS_PER_TABLE` plus uppercase extras.
- 2026-04-25 verification: `load/test/scale_test.rb`, `load/test/adapter_client_test.rb`, `load/test/cli_test.rb`, `load/test/workload_registry_test.rb`, `load/test/runner_test.rb`, and `workloads/missing_index_todos/test/workload_test.rb` all passed under `BUNDLE_GEMFILE=collector/Gemfile bundle exec ruby ...`.
- 2026-04-25 invariant policy docs: `run` and `soak` now document `--invariants enforce|warn|off`, while `verify-fixture` explicitly rejects the flag.
- 2026-04-25 invariant policy verification: `--invariants off` disables runtime invariant sampling only; it does not bypass `missing-index-todos` fixture verification, so `run` and `soak` still require `DATABASE_URL` for the pre-flight verifier.
- 2026-04-25 Task 1 follow-up: `Load::Scale` now rejects reserved extras with `key.to_s.downcase`, and `scale_test` asserts the exact `env_pairs.to_a` order for `ROWS_PER_TABLE`, `OPEN_FRACTION`, and `BATCH_SIZE`.
- 2026-04-25 Task 2 generic invariant contract: `Load::Runner` now persists schema version `2`, models invariant samples as arrays of named `InvariantCheck` records, and `Load::Workload` exposes a base `invariant_sampler(database_url:, pg:)` hook that defaults to `nil`; the missing-index workload-specific sampler remains intentionally unimplemented for the next task.
- 2026-04-25 Task 3 sampler extraction: `workloads/missing_index_todos/invariant_sampler.rb` now owns the todo-count SQL and builds generic `Load::Runner::InvariantSample` checks for `open_count` and `total_count`; the workload now constructs it from `rows_per_table` thresholds, and runner-side isolated-PG coverage moved into the workload sampler tests.
- 2026-04-25 Task 2 monitor extraction: `Load::InvariantMonitor` owns the sampling thread, `sample_once` stays public, `stop` clears cached sampler failures before raising, and the runner callback must expand `sample.to_record(...)` into keywords for `Load::RunState#append_invariant_sample`.
- 2026-04-25 Task 2 monitor extraction follow-up: the monitor is continuous-mode only in `Load::Runner`, and the cooperative stop test passes by raising into the sleeping sampler thread rather than waiting for the full interval.
- 2026-04-25 Task 2 follow-up regression: `InvariantMonitor#stop` needs a focused test for the `ThreadError` race when the target thread exits after `sleeping?` is true but before `thread.raise`; the fake thread in that test must use `Kernel.raise` to avoid recursive self-calls.
- 2026-04-25 Task 3 execution extraction: `Load::LoadExecution` now owns worker construction, reporter lifecycle, finite/continuous wait loops, and bounded worker drain; it must trigger `stop_flag.trigger(:timeout)` when a finite deadline expires so workers shut down cooperatively instead of being torn down only by the drain timeout.
- 2026-04-25 Task 4 runner cutover: `Load::Runner` no longer has a default invariant sampler or todo SQL; it now asks the workload for `invariant_sampler(database_url:, pg:)`, raises `continuous mode requires the workload to provide an invariant sampler` when continuous mode gets `nil`, and keeps invariant breach handling generic.
- 2026-04-25 Task 4 verification fix: `load/test/cli_test.rb` needed its temporary soak workload to provide a trivial invariant sampler after the runner stopped manufacturing one, and generic `runner_test` fixture workloads no longer need ceremonial `open_fraction` extras.
- 2026-04-25 constructor refactor design: `Load::Runner` should regroup its flat keyword args into `Runtime`, `Config`, and `InvariantConfig`; keep `verifier` in `Config` for now, and move invariant sampler resolution/validation out of the main constructor body into small helper methods.
- 2026-04-25 constructor grouping refactor: `Load::Runner` now takes `runtime`, `config`, and `invariant_config` value objects; this keeps runtime seams, run settings, and invariant setup separate without introducing a factory or generic options hash.
- 2026-04-25 tenant-shaped workload design: `missing-index-todos` should read `USER_COUNT` from `scale.extra`, make list/search/write actions user-scoped, make `close_todo` fetch one user’s open candidates before patching, keep `delete_completed_todos` direct per-user, and move verifier ownership entirely into the workload rather than leaving todo-specific checks in `load/lib/load`.
- 2026-04-25 tenant-shaped workload Task 1: `missing-index-todos` now sets `scale.extra[:user_count] = 1_000`; list/search/write actions sample `user_id` from `scale.extra`, `close_todo` prefetches one sampled user's open todos and returns a `204` no-op when none exist, and `delete_completed_todos` is a direct query-string delete with no prefetch.
- 2026-04-25 Task 1 regression note: `load/test/runner_test.rb` mixed-write support must model the new tenant-shaped contracts too; without `user_count` in the synthetic scale and a JSON body for `close_todo`'s open-todos prefetch, the runner test times out instead of producing diverse ids.
- 2026-04-25 Task 1 review follow-up: `close_todo` must choose among a sampled user's returned open todo ids with the worker RNG (`ids.sample(random: rng)`); the focused action test now locks the seeded `[17, 23] -> 23` case instead of baking in `first`.
- 2026-04-25 Task 1.5 adapter query-id capture: the live normalized `pg_stat_statements.query` for `user.todos.with_status("open").ordered_by_created_desc.page(1, 50).load` in `/home/bjw/db-specialist-demo` is `SELECT "todos".* FROM "todos" WHERE "todos"."user_id" = $1 AND "todos"."status" = $2 ORDER BY "todos"."created_at" DESC, "todos"."id" DESC LIMIT $3 OFFSET $4`.
- 2026-04-25 Task 1.5 local note: `/home/bjw/db-specialist-demo` is still on `wip/mixed-todo-api` with a stale `api_index` controller, but the association-based warm-up query is already sufficient to capture the tenant-scoped statement family for adapter query-id attribution.
- 2026-04-25 Task 3 verifier boundary: `Load::VerificationError` is the only core load verification type now; the todo-specific preflight logic lives in `workloads/missing_index_todos/verifier.rb`, and `load/lib/load/fixture_verifier.rb` plus its core test were removed entirely.
- 2026-04-25 Task 3 search fixture: `fixtures/mixed-todo-app/search-explain.json` was re-captured from local `checkpoint_demo` with `EXPLAIN (FORMAT JSON) SELECT * FROM todos WHERE user_id = 1 AND title LIKE '%foo%' ORDER BY created_at DESC, id DESC LIMIT 50`, yielding a `Sort Key` of `["created_at DESC", "id DESC"]` and a filter of `(((title)::text ~~ '%foo%'::text) AND (user_id = 1))`.
- 2026-04-25 Task 3 verification note: `load/test/runner_test.rb` passed reliably when run alone, but `test_runner_persists_invariant_samples_in_run_record` can fail with `no_successful_requests` when the runner and CLI suites are executed in parallel because the continuous invariant thread can beat the worker to the first success.
- 2026-04-26 verifier fixture refresh: `fixtures/mixed-todo-app/search-explain.json` now matches the live `checkpoint_demo` tenant search plan shape (`Limit -> Sort -> Bitmap Heap Scan -> Bitmap Index Scan on index_todos_on_user_id`), and `workloads/missing_index_todos/test/verifier_test.rb` has a fixture-lock test so stale seq-scan references fail before live preflight.
- 2026-04-26 dominance tuning: lowering `scale.extra[:user_count]` from `1_000` to `100` was not enough to restore oracle dominance because `/api/todos/counts` still drives the per-user `SELECT COUNT(*) FROM "todos" WHERE "todos"."user_id" = $1` family above the primary open-todos query. For now `FetchCounts` is weighted to `0` so the tenant-pathology gate can pass; revisit the counts/N+1 balance in a later phase instead of treating it as a requirement in this round.
- 2026-04-26 invariant monitor Task 2/3: `Load::InvariantMonitor` now groups policy and effects into nested `Config`, `Sink`, and `State`, keeps policy branching in `sample_once`, uses `State#with_sleeping` only for sleep-flag toggling under the existing `Thread.handle_interrupt` discipline, and preserves clear-and-raise-once sampler failure handling while the runner builds the grouped constructor args.
- 2026-04-26 invariant monitor validation follow-up: `Load::InvariantMonitor::Config` now rejects policies outside `:off`, `:warn`, and `:enforce` with `ArgumentError`, so callers that bypass CLI parsing fail fast instead of silently constructing a monitor with undefined policy behavior.
- 2026-04-26 soak setup: `docker compose down --volumes --remove-orphans --rmi local` plus `docker compose build --no-cache` rebuilt a clean `checkpoint-collector` stack; the first recreate failed because `.worktrees/mixed-todo-fixture-design-parallel` was still publishing `5432`, `8123`, and `9000`, and a later `docker compose up -d --force-recreate` was required to attach host port bindings after the failed create.
- 2026-04-26 soak bugfix: live soak metrics exposed `close_todo` as `TypeError` because `/api/todos` returns an `{ "items": [...] }` envelope while the action parsed the body as a bare array; `CloseTodo` now reads `items`, and the fresh background soak `runs/20260426T164046Z-missing-index-todos` showed `132` close-todo calls with `0` close-todo errors across the last five metric intervals checked.
- 2026-04-26 PlanetScale soak design notes: PlanetScale Postgres supports `pg_stat_statements` after dashboard activation and `CREATE EXTENSION`, but the current local collector also tails Docker-mounted Postgres JSON logs, so remote soak should make stats polling usable without local log ingestion. Brett explicitly deferred branch-per-run; current design scope is reset/reseed against an existing PlanetScale branch, with branch automation tracked as future work.
- 2026-04-26 PlanetScale logs decision: PlanetScale has Cluster Logs and Query Insights/`pginsights`, and pganalyze can collect PlanetScale logs with a service token, but Brett agreed not to block the reset/reseed pass on remote log ingestion. Current implementation should be stats-only for PlanetScale, with logs/Insights integration tracked as a fast follow.
- 2026-04-26 PlanetScale reset checkpoint: remote reset-state against the existing branch succeeded after enabling `pg_stat_statements` in the PlanetScale dashboard. Local libpq 18.0.1 failed certificate verification with `sslrootcert=system`, but `sslmode=verify-full&sslrootcert=/etc/ssl/certs/ca-certificates.crt` worked. Reset seeded `100000` todos with `59859` open todos, captured one query id (`-5699193110986258845`), and updated `pg_stat_statements_info.stats_reset` at `2026-04-26 18:25:29.285509+00`.
- 2026-04-26 PlanetScale collector checkpoint: stats-only collector pass against PlanetScale wrote rows to the existing `checkpoint-collector-clickhouse-1` instance on localhost because this worktree could not bind `8123`. `query_events` increased from `65043` to `65341`, latest `collected_at` became `2026-04-26 18:35:45.093`, and no Postgres log file was read. The plan's `ruby -r./collector/bin/collector` command does not load a relative file here; use `ruby -e 'load "./collector/bin/collector"; ...'` instead.
- 2026-04-26 PlanetScale local verification: `make test-load` passed with `156 runs, 528 assertions`; `make test-adapters` passed with `26 runs, 75 assertions, 2 skips`; `make test-workloads` passed with `52 runs, 215 assertions`; and `BUNDLE_GEMFILE=collector/Gemfile bundle exec ruby -e 'Dir["collector/test/*_test.rb"].sort.each { |path| load path }'` passed with `55 runs, 171 assertions`.
